#include<iostream>
using namespace std;

int main(){
    int a,b,c,d,e,f,h,i,k,l,x;
    cout << "Enter 10 numbers: ";
    cin >> a >> b >> c >> d >> e >> f >> h >> i >> k >> l;
    x = (a + b + c + d + e + f + h + i + k + l) / 10;
    cout << "Result: " << x << endl;
    return 0;
}